import { createSlice } from '@reduxjs/toolkit';

const repeatWordSlice = createSlice({
  name: 'repeatWord',
  initialState: {
    done: false,
  },
});

export const {} = repeatWordsSlice.actions;

export default repeatWordsSlice.reducer;
